<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Download Page</title>
</head>
<style>
    input[type=email], select {
      width: 50%;
      padding: 12px 20px;
      margin: 8px 0;
      display: inline-block;
      border: 1px solid #ccc;
      border-radius: 4px;
      box-sizing: border-box;
    }
    input[type=text], select {
      width: 50%;
      padding: 12px 20px;
      margin: 8px 0;
      display: inline-block;
      border: 1px solid #ccc;
      border-radius: 4px;
      box-sizing: border-box;
    }
    
    input[type=submit] {
  width: 20%;
  background-color: #2a47a7;
  color: white;
  padding: 14px 20px;
  margin: 8px 0;
  border: none;
  border-radius: 4px;
  cursor: pointer;
}
    
    div {
      border-radius: 5px;
      background-color: #f2f2f2;
      padding: 20px;
    }
    </style>
<body>
    <h1>Hi, </h1>
    <p>Here is your Download Link:</p>
    <a href="<%= download %>" target="_blank">
        <%= download %>
    </a>
    <p>Click on the link to download your file</p>
    <h5>You can send the link via email</h5>
        <form action="<%= process.env.APP_BASE_URL %>/files/download/email/" method="post" >
            <h2>Enter to send email</h2>
            <label for="sender">Enter Sender email</label>
            <input type="email" name="sender" id="sender"><br><br>
            <label for="receiver">Enter Receiver email</label>
            <input type="email" name="receiver" id="receiver"><br>
            <label for="downloadLink">Link</label>
            <input type="text" name="downloadLink" id="downloadLink" value="<%= download %>"><br>
            <input type="submit" value="Submit">
        </form>

</body>

</html>